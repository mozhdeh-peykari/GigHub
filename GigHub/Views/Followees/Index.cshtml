@model IEnumerable<GigHub.Models.ApplicationUser>

@{
    ViewBag.Title = "Following";
}

<h2>Following</h2>

<ul>
    @foreach(var artist in Model)
    {
        <li>
            @artist.Name
        </li>
    }
</ul>

